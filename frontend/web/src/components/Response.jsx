import React from 'react';

const Response = ({ answer }) => {
    return (
        <div>
            <h2>Response:</h2>
            <p>{answer}</p>
        </div>
    );
};

export default Response;
